# The canonical docker-compose.yml has moved to the repository root.
# Use it from there:
#
#   cp .env.example .env
#   docker compose up -d
#
# This file is kept as a placeholder so existing links do not 404.
# See ../../docker-compose.yml and ../../.env.example.
